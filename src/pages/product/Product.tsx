import { Container } from '@mui/material';
import AllProducts from './AllProducts';

const Product = () => {
    return (
        <Container>
            <AllProducts />
        </Container>
    );
};

export default Product;